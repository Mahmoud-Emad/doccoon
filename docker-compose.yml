version: '3.8'

services:
  openbook:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openbook
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

